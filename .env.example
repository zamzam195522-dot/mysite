# Postgres connection string (PRODUCTION)
# Example formats:
# - postgres://USER:PASSWORD@HOST:PORT/DBNAME
# - postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=

# If your hosted Postgres requires SSL (most do), set one of:
# - add `?sslmode=require` in DATABASE_URL
# - OR set this flag to true
DATABASE_SSL=

# If you ever use a separate backend, set the base URL here
NEXT_PUBLIC_API_BASE_URL=

# Postgres connection string (recommended)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/water_site

# Optional: if you run a separate backend later
NEXT_PUBLIC_API_BASE_URL=

